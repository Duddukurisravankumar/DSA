#include<bits/stdc++.h>
using namespace std;
int main() {
    int num ,sum=0;
    cout << "enter the numbers to be add: ";
    while (cin >> num){
        sum += num;
    } 
    cout << "the sum of given numbers are = "<< sum << endl;
}
